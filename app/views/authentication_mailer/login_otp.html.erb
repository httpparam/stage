<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
    <style>
      @import url('https://api.fontshare.com/v2/css?f[]=clash-display@400,500,600,700&display=swap');
      @font-face {
        font-family: 'Clash Display';
        src: url('https://api.fontshare.com/v2/fonts/clash-display/400.woff2') format('woff2');
        font-weight: 400;
      }
      @font-face {
        font-family: 'Clash Display';
        src: url('https://api.fontshare.com/v2/fonts/clash-display/600.woff2') format('woff2');
        font-weight: 600;
      }
      @font-face {
        font-family: 'Clash Display';
        src: url('https://api.fontshare.com/v2/fonts/clash-display/700.woff2') format('woff2');
        font-weight: 700;
      }
      /* Catppuccin Mocha Colors */
      body {
        font-family: 'Clash Display', 'Trebuchet MS', 'Verdana', sans-serif;
        background-color: #1e1e2e;
        margin: 0;
        padding: 40px 20px;
      }
      .container {
        max-width: 480px;
        margin: 0 auto;
        background-color: #313244;
        border-radius: 12px;
        padding: 48px 40px;
      }
      .logo {
        font-size: 28px;
        font-weight: 700;
        color: #cdd6f4;
        text-align: center;
        margin-bottom: 8px;
      }
      .subtitle {
        font-size: 14px;
        color: #a6adc8;
        text-align: center;
        margin-bottom: 40px;
      }
      .message {
        color: #bac2de;
        font-size: 16px;
        line-height: 1.6;
        text-align: center;
        margin-bottom: 32px;
      }
      .code-wrapper {
        text-align: center;
        margin: 32px 0;
      }
      .code {
        display: inline-block;
        font-size: 36px;
        font-weight: 700;
        letter-spacing: 12px;
        padding: 20px 32px;
        background-color: #45475a;
        border-radius: 8px;
        color: #cba6f7;
      }
      .expires {
        color: #6c7086;
        font-size: 14px;
        text-align: center;
        margin-top: 32px;
      }
      .footer {
        color: #6c7086;
        font-size: 13px;
        text-align: center;
        margin-top: 40px;
        padding-top: 24px;
        border-top: 1px solid #45475a;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo">Stage</div>
      <div class="subtitle">Passwordless Authentication</div>

      <p class="message">
        Use this code to sign in to your account.
      </p>

      <div class="code-wrapper">
        <span class="code"><%= @otp_code %></span>
      </div>

      <p class="expires">
        This code expires in 10 minutes.
      </p>

      <p class="footer">
        If you didn't request this code, you can safely ignore this email.
      </p>
    </div>
  </body>
</html>
